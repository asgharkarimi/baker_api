# راهنمای رفع مشکل Media Picker

## مشکل: دکمه‌های افزودن عکس/ویدیو کار نمی‌کنن

### ✅ چک‌لیست اول

1. **Permissions چک کن:**
   - فایل `android/app/src/main/AndroidManifest.xml` رو باز کن
   - مطمئن شو این خطوط وجود دارن:
   ```xml
   <uses-permission android:name="android.permission.CAMERA"/>
   <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
   <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
   <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
   ```

2. **برنامه رو Uninstall کن:**
   ```bash
   flutter clean
   flutter run
   ```
   یا از گوشی/امولاتور برنامه رو پاک کن و دوباره نصب کن

3. **Permission در زمان اجرا:**
   - وقتی روی "افزودن عکس" کلیک می‌کنی
   - باید یک دیالوگ permission بیاد
   - اگه نیومد، به Settings > Apps > MyBakersJobApp > Permissions برو
   - دسترسی Camera و Storage رو فعال کن

### 🔍 تست کردن

بعد از کلیک روی دکمه:

1. **دیالوگ انتخاب منبع باید بیاد:**
   - دوربین
   - گالری

2. **اگه دیالوگ نیومد:**
   - به terminal نگاه کن
   - باید ببینی: `Error picking image: ...`
   - اون خطا رو بهم بگو

3. **اگه دیالوگ اومد ولی بعد از انتخاب هیچی نشد:**
   - باید یک SnackBar سبز ببینی: "1 عکس انتخاب شد"
   - اگه ندیدی، مشکل از callback هست

### 🐛 Debug Mode

الان کد رو طوری تغییر دادم که:
- هر بار عکس انتخاب میشه، یک پیام نشون میده
- در terminal هم log میزنه

**تست کن:**
1. روی "افزودن عکس" کلیک کن
2. یک عکس انتخاب کن
3. باید ببینی:
   - در terminal: `Images changed: 1`
   - در برنامه: پیام "1 عکس انتخاب شد"
   - عکس در Grid نمایش داده بشه

### 📱 مشکلات رایج

#### مشکل 1: Permission Denied
**علامت:** هیچ اتفاقی نمی‌افته
**راه حل:**
```bash
# پاک کردن کامل
flutter clean
# حذف از گوشی
adb uninstall com.example.my_bakers_jobapp
# نصب مجدد
flutter run
```

#### مشکل 2: دیالوگ نمیاد
**علامت:** کلیک می‌کنی ولی دیالوگ باز نمیشه
**راه حل:** چک کن که `_showSourceDialog` درست کار می‌کنه

#### مشکل 3: عکس انتخاب میشه ولی نمایش داده نمیشه
**علامت:** دیالوگ میاد، عکس انتخاب میشه، ولی در لیست نیست
**راه حل:** مشکل از `setState` هست

### 🔧 تست دستی

اگه همچنان کار نکرد، این کد ساده رو امتحان کن:

```dart
ElevatedButton(
  onPressed: () async {
    final picker = ImagePicker();
    final image = await picker.pickImage(source: ImageSource.gallery);
    if (image != null) {
      print('Image picked: ${image.path}');
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(content: Text('عکس انتخاب شد: ${image.path}')),
      );
    } else {
      print('No image selected');
    }
  },
  child: Text('تست ساده'),
)
```

اگه این کار کرد، مشکل از `MediaPickerWidget` هست.
اگه این هم کار نکرد، مشکل از permissions هست.

### 📞 اگه باز کار نکرد

بهم بگو:
1. چه پیامی در terminal میاد؟
2. دیالوگ permission میاد؟
3. دیالوگ انتخاب منبع (دوربین/گالری) میاد؟
4. نسخه Android امولاتور/گوشی چنده؟

با این اطلاعات می‌تونم دقیق‌تر کمک کنم! 🚀
